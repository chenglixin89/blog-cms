server:
  port: 8080

spring:
  application:
    name: blog-backend
  datasource:
    url: jdbc:mysql://localhost:3306/blog_cms?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

blog:
  upload:
    dir: uploads
  auth:
    admin:
      username: admin
      password: "{noop}123456"
      nickname: Admin
  jwt:
    secret: "this-is-a-demo-secret-key-change-it-at-least-32-chars"
    expire-seconds: 7200
  crypto:
    # AES-256 key material. Hashed with SHA-256 to derive the actual key, so any
    # >=32-byte UTF-8 string works. Override in production via BLOG_CRYPTO_SECRET;
    # generate with `openssl rand -base64 48`.
    secret: "this-is-a-demo-aes-256-secret-key-change-it-at-least-32-bytes-long"

