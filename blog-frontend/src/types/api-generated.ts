/**
 * Generated stub for the backend OpenAPI typings.
 *
 * The real content of this file is produced by `npm run gen:api`, which
 * downloads /v3/api-docs from a running backend (springdoc-openapi) and
 * passes it through openapi-typescript. Until you run that script the
 * file just exports an empty `paths` / `components` namespace so the
 * rest of the front-end keeps compiling.
 *
 * To refresh:
 *
 *   1. Start the backend (default http://localhost:8080).
 *   2. From `blog-frontend/`:
 *        npm run gen:api
 *      Override the source URL when needed:
 *        BLOG_API_DOCS_URL=http://staging.example.com/v3/api-docs npm run gen:api
 *   3. Commit the regenerated file.
 *
 * Do not hand-edit this file: every regeneration overwrites it.
 */

export interface paths {}
export interface components {
  schemas: Record<string, never>;
}
export type operations = Record<string, never>;
